# sig-frx runtime dependencies.
#
# Pulled from the Fractalyze package index alongside upstream PyPI:
#   pip install -r requirements.in \
#     --extra-index-url https://fractalyze.github.io/pypi/simple/
#
# hash-frx — the symmetric layer every scheme here hashes through — is absent on
# purpose: it is a Bazel module, pinned by commit in //MODULE.bazel, not a wheel.
#
# frx moves with the hash-frx pin, never alone: hash-frx resolves frx from its
# own lock, so skew puts two frx copies on one test's sys.path and surfaces as a
# dtype error rather than a version conflict. zk_dtypes is pinned here rather
# than left to frx's resolution, so the floor this repo needs is stated.
absl-py
numpy
frx==0.10.1.dev20260809035234
frxlib==0.10.1.dev20260809035234
frx-cuda12-pjrt==0.10.1.dev20260809035234
frx-cuda12-plugin==0.10.1.dev20260809035234
zk-dtypes==0.0.14
